# Приклад Розширення Мавки

Збудувати:

```shell
sh build.sh
```

Буде в директорії `готове`.

Скопіювати `готове/привіт` в папку проєкта Мавки `розширення` після чого підключити так:

```мавка
взяти розширення привіт

друк(привіт.результат) ;; 5
```